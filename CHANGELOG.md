# Changelog

Chrome 网上应用店已发布后的版本记录。扩展版本见 `package.json` / `manifest.json`。  
后续每次面向商店或主干面板的优化，都在本文件追加条目。

商店控制台「最新动态」可粘贴各版本下的 **商店文案**。

## 3.3.36 — 2026-08-17

### 商店文案

```
Cocos 3.x 支持 3D Mesh 拾取与 AABB 线框高亮。
```

### 变更

- 3.x 拾取增加 Mesh / SkinnedMesh 射线 vs AABB（不依赖物理模块）
- 悬停/选中 3D 节点时画青色 AABB 投影线框
- 小块 UI（按钮等）仍优先于 3D；整屏 Canvas 不再抢走 3D 命中

## 3.3.35 — 2026-08-17

### 商店文案

```
修复 Cocos 3.x 拾取框错位（高分屏下缩到左半边）。
```

### 变更

- 3.x 拾取/描边改为使用场景里的 UI 相机（不限节点名 `UICamera`）
- `worldToScreen` 按相机 window 归一化到 CSS，不再把世界坐标再乘一遍 DPR
- 命中检测改为 `screenToWorld` 后按世界包围盒判断

## 3.3.34 — 2026-08-17

### 商店文案

```
Cocos 2.x/3.x 工具栏增加拾取与下载（纹理/Spine/BMFont）。
```

### 变更

- 主干 2.x / 3.x 工具栏增加 **拾取**：点击画面节点，展开祖先并定位到节点树
- 主干 2.x / 3.x 工具栏增加 **下载**：选中节点纹理 / 子树 Sprite / Spine / BMFont，
  以及整场景 Sprite PNG（Chrome 目录选择）
- 工具栏顺序：刷新、暂停、拾取、下载、搜索、收起（收起仍靠右且不被裁切）

## 3.3.33 — 2026-08-14

### 商店文案

```
MCP 未连接时可点击指示灯，查看 GitHub 安装指引。
```

### 变更

- 未连接时点击右上角 MCP 指示灯，弹出本机桥安装步骤
- 指引内含仓库地址 https://github.com/shinjiyu/CososInspector，可复制或打开
- Creator 3.x / 2.x、Egret、Pixi 面板行为一致

## 3.3.32 — 2026-08-14

### 商店文案

```
主干工具栏去掉「扫描 DC / 清除 / 资源」，「收起」不再被裁切。
```

### 变更

- 主干 Inspector 工具栏只保留：刷新、暂停、节点搜索、收起
- 去掉「标准」粒度、「扫描 DC」、「清除」、「资源」按钮
- 「收起」固定在工具栏右侧且 `flex-shrink: 0`，窄面板下完整可见
- 状态栏不再显示「基准 NaN DC」等扫描结果

### 说明

- DC 扫描与资源浮窗源码仍保留（`perfScan.ts` / `assetPanel.ts`），面板不再暴露入口
- 资源浮窗依赖 `cc.assetManager`，多数试玩包不兼容

## 3.3.31 — 2026-08

Chrome 网上应用店首发版本（基线）。
